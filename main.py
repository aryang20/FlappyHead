import cv2 as cv, pygame as pg, mediapipe as mp, sys, time, random
import numpy

pg.init()

screenWidth = 800
screenHeight = 800

screen = pg.display.set_mode([screenWidth, screenHeight])


















